#include <stdio.h>

/*  Premier
    commentaire
    multi-lignes
*/
int main(int argc, char* argv[]) {
    int distance = 100;

    // Une deuxième façon d'écrire un commentaire mais cette fois sur une seule ligne.
    printf("You are %d miles away.\n", distance);

    return 0;
}